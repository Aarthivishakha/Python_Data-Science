name: semgrep-bandit
language: python
language_version: "3.14"
analysis_runtime: "Python >=3.10"
target_files: ["datascience/**/*.py"]
trigger:
  command: "semgrep scan --config auto datascience && bandit -r datascience"
